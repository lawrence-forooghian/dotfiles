#!/bin/bash
set -e

# cd so asdf picks up the dotfiles .tool-versions (asdf doesn't offer a
# mechanism for using a .tool-versions file unrelated to the working directory).
cd ~/dotfiles

asdf exec bundle exec "$@"
