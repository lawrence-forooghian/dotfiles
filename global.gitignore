## "junk" that I’m extremely rarely interested in; this should be kept in sync with g:NERDTreeIgnore in vimrc

# Created by macOS
.DS_Store
# Vim swap files
*.sw?

## Files I use to control per-repo behaviour of tools that aren’t widely used

.ackrc
.projections.json

# Created by Visual Studio Code
.vscode

# Local Claude settings (e.g. allowed commands) for a repo or a sub-project
**/.claude/settings.local.json
